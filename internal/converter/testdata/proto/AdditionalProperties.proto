syntax = "proto3";
package samples;

message AdditionalProperties {
    string key = 1;

    message NestedMessage {
        string nested_key = 1;
    }

    NestedMessage message = 2;
    repeated int32 repeated_primitive = 3;
    repeated NestedMessage repeated_message = 4;
}
